{
  "name": "gitlyte-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Instantly turn your GitHub repo into a live website — no code needed.",
  "author": "wadakatu",
  "license": "ISC",
  "homepage": "https://github.com//",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "start": "pnpm --filter @gitlyte/core start",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix",
    "format": "pnpm -r run format",
    "format:fix": "pnpm -r run format:fix",
    "check": "pnpm -r run check",
    "check:fix": "pnpm -r run check:fix",
    "ci:check": "pnpm --filter @gitlyte/core ci:check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@lhci/cli": "^0.15.1",
    "@types/node": "^20.0.0",
    "promptfoo": "^0.120.8",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "overrides": {
      "@types/pg": "8.15.1"
    }
  },
  "engines": {
    "node": ">= 24"
  },
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.55.1"
  }
}
