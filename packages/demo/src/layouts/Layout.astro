---
import BaseLayout from '@gitlyte/shared/components/Layout/BaseLayout.astro';

export interface Props {
  title: string;
  description?: string;
  layoutType?: string;
}

const { title, description, layoutType } = Astro.props;
---

<BaseLayout title={title} description={description} layoutType={layoutType}>
  <slot />
</BaseLayout>